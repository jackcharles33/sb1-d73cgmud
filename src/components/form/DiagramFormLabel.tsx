import { styled, Typography } from '@mui/material';

export const DiagramFormLabel = styled(Typography)({
  color: '#000',
  fontWeight: 600,
  fontSize: '0.95rem',
  marginBottom: '8px',
  fontFamily: 'Montserrat, sans-serif'
});